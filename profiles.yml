# No environment-specific values or secrets are committed here.
# Copy .env.template to .env, fill it in, then export it before running dbt.

st_bytes_medical_center:
  target: dev
  outputs:
    dev:
      type: databricks
      host: "{{ env_var('DATABRICKS_HOST') }}"
      http_path: "{{ env_var('DATABRICKS_HTTP_PATH') }}"
      catalog: "{{ env_var('DATABRICKS_CATALOG_DEV') }}"
      schema: "{{ env_var('DATABRICKS_SCHEMA_DEV') }}"
      token: "{{ env_var('DBT_ACCESS_TOKEN') }}"
      threads: 16

    prod:
      type: databricks
      host: "{{ env_var('DATABRICKS_HOST') }}"
      http_path: "{{ env_var('DATABRICKS_HTTP_PATH') }}"
      catalog: "{{ env_var('DATABRICKS_CATALOG_PROD') }}"
      schema: "{{ env_var('DATABRICKS_SCHEMA_PROD') }}"
      token: "{{ env_var('DBT_ACCESS_TOKEN') }}"
      threads: 16