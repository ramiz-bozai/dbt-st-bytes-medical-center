# UC Metrics Views

| File | Purpose |
|------|---------|
| `01_team_goals_metrics_view.sql` | Metrics view over dbt mart `goals` |

Create after gold tables exist. Query with `SELECT * FROM catalog.schema.team_goals_metrics`.
