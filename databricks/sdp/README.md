# SDP — bronze ingest

| Pipeline | Dataset |
|----------|---------|
| `st_bytes_bronze_pipeline.py` | St. Bytes Medical Center (Synthea) — **use this** |
| `nba_bronze_pipeline.py` | Legacy NBA demo (deprecated) |

Configure `CATALOG`, `BRONZE_SCHEMA`, and `BATCH_PATH` before running as a Lakeflow Pipeline.
